<h2>Monthly Cashflow Summary</h2>


<table border="1">
  <thead>
  <tr>
    <th>Type</th>
    <% @monthly_data.keys.each do |month| %>
      <th><%= month %></th>
    <% end %>
  </tr>
  </thead>
  <tbody>
  <% @row_labels.each do |label| %>
    <tr>
      <td><%= label.presence || "Unspecified" %></td>
      <% @monthly_data.each do |month, types| %>
        <td>
          <% if types[label] %>
            <%= Money.new(types[label] * 100, "EUR").format %>
          <% else %>
            0.00€
          <% end %>
        </td>
      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>

